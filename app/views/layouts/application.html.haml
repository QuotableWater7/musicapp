!!!
%html
  %head
    %title Self Taught Music
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .app-wrapper
      = render 'layouts/header'
      .container
        .app-content
          .row
            .col-md-12
              - flash.each do |key, value|
                - key = key == 'error' ? 'danger' : key
                %div{class: "app-alert alert alert-#{key}"}= value.html_safe
          = yield
